name: voice_message_package
description: Modified the exisiting https://pub.dev/packages/voice_message_package to better suit our needs.
version: 1.0.0+1
homepage: https://github.com/Diverge-World/updated_voice_message_package.git

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.1
  just_audio: ^0.9.36
  syncfusion_flutter_sliders: ^23.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
